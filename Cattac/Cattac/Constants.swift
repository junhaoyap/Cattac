/*
    Where we keep all the constants
*/

import UIKit
import Foundation

struct Constants {
    struct BasicLevel {
        static let numRows: Int = 10
        static let numColumns: Int = 10
    }
}